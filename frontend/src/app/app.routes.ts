import { Routes } from '@angular/router';
import { GlobalWatcherApp } from './global-watcher-app/global-watcher-app';

export const routes: Routes = [
    { path: 'global-watch', component: GlobalWatcherApp }  
];
